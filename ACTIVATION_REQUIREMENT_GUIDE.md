# 🎮 نظام طلب التفعيل الإجباري

## 📌 نبذة عن النظام

تم تطوير نظام إجباري لضمان أن جميع المستخدمين **يجب عليهم رفع طلب تفعيل قبل اللعب**.

### الآلية:
1. **عند الدخول:** يتم فحص حالة التفعيل تلقائياً
2. **إذا لم يتم تفعيله:** يرى modal إجباري لا يمكن تجاوزه
3. **رفع الطلب:** يرفع طلب تفعيل
4. **الانتظار:** الفحص التلقائي كل 5 ثوانٍ للموافقة
5. **الموافقة:** عند موافقة Admin، يتم تحديث الصفحة تلقائياً

---

## 🔍 كيفية الفحص

### في `app/page.tsx`:

```typescript
// الفحص الأولي
useEffect(() => {
  const check = async () => {
    if (!session?.user?.id) {
      setIsApproved(false)
      return
    }
    
    // Admin bypass
    const adminIds = ["897450827353063505", "1186739142231605248"]
    if (adminIds.includes(session.user.id)) {
      setIsApproved(true)
      return
    }
    
    // فحص طلب التفعيل
    const res = await fetch("/api/activation-requests?mine=1")
    const data = await res.json()
    setIsApproved(data?.status === "approved")
  }
  void check()
}, [session?.user?.id])
```

### في `components/activation-request-modal.tsx`:

```typescript
// فحص دوري تلقائي كل 5 ثوانٍ
useEffect(() => {
  if (!session?.user?.id || statusState === "approved") return

  const interval = setInterval(() => {
    checkStatus()
    setCheckCount((c) => c + 1)
  }, 5000)

  return () => clearInterval(interval)
}, [session?.user?.id, statusState])
```

---

## 📊 حالات النظام

### 1️⃣ **غير مفعل (Not Approved)**
```
الحالة: isApproved === false
العرض: Modal التفعيل (إجباري، لا يمكن إغلاقه)
الخيارات:
  - تسجيل الدخول (إذا لم يسجل)
  - رفع طلب تفعيل
```

### 2️⃣ **في الانتظار (Pending)**
```
الحالة: isApproved === false + statusState === "pending"
العرض: رسالة انتظار + فحص دوري تلقائي
التحديث: كل 5 ثوانٍ
النص: "طلبك قيد المراجعة..."
```

### 3️⃣ **معتمد (Approved)**
```
الحالة: isApproved === true
العرض: اللعبة الكاملة مع جميع الخيارات
الوصول: كامل الوصول للعبة والفئات والأسئلة
```

### 4️⃣ **في الفحص (Checking)**
```
الحالة: isApproved === null
العرض: شاشة تحميل "جاري فحص التفعيل..."
المدة: حتى انتهاء الفحص من API
```

### 5️⃣ **Admin Bypass**
```
الحالة: Admin IDs: ["897450827353063505", "1186739142231605248"]
التأثير: يتخطى الفحص مباشرة للعبة
```

---

## 🚀 تدفق المستخدم

```
┌─────────────────────────────────────┐
│   المستخدم يفتح الموقع              │
└────────────┬────────────────────────┘
             │
             ↓
┌─────────────────────────────────────┐
│   هل يملك حساب Discord معتمد؟       │
└────────────┬────────────────────────┘
             │
        NO   │   YES
    ┌────────┴────────┐
    │                 │
    ↓                 ↓
┌──────────┐   ┌──────────────────────┐
│تسجيل     │   │هل لديه طلب تفعيل؟    │
│الدخول    │   │                      │
│(Discord) │   └─────────┬──────────┘
└────┬─────┘             │
     │            YES    │    NO
     │         ┌─────────┴──────────┐
     │         │                    │
     ↓         ↓                    ↓
     └────→┌──────────┐    ┌─────────────┐
           │Modal شاشة│→   │هل معتمد؟    │
           │          │    │             │
           │التفعيل   │    └──┬──────┬──┘
           │          │       │      │
           │رفع طلب   │   YES │  NO  │
           │          │       │      │
           └────┬─────┘       │      │
                │        ┌────┘      │
                │        ↓           │
                │   ┌────────────┐   │
                │   │اللعبة ✅    │   │
                │   │(معتمد)     │   │
                │   └────────────┘   │
                │                    │
                │              ┌─────┴──────────┐
                │              │  فحص دوري     │
                →──────────────→  (5 ثوانٍ)    │
                               │ عند الموافقة  │
                               │ (refresh)     │
                               └────┬──────────┘
                                    │
                                    ↓
                            ┌─────────────────┐
                            │ اللعبة ✅      │
                            │ (معتمد الآن)   │
                            └─────────────────┘
```

---

## 💾 البيانات المخزنة

### ملف: `data/activation-requests.json`

```json
[
  {
    "id": "req-1710758940123-abc123",
    "userId": "123456789",
    "username": "أحمد",
    "status": "pending",
    "createdAt": "2026-03-18T10:00:00.000Z"
  },
  {
    "id": "req-1710758940124-def456",
    "userId": "987654321",
    "username": "محمد",
    "status": "approved",
    "approvedAt": "2026-03-18T10:05:00.000Z",
    "approvedBy": "897450827353063505"
  }
]
```

---

## 🔧 API Endpoints

### `GET /api/activation-requests?mine=1`
رؤية طلب التفعيل الخاص بك

**Response:**
```json
{
  "id": "req-xxx",
  "userId": "xxx",
  "username": "xxx",
  "status": "pending|approved",
  "createdAt": "2026-03-18T..."
}
```

### `POST /api/activation-requests`
رفع طلب تفعيل جديد

**صاحب الطلب مطلوب: ✅ نعم (يجب تسجيل الدخول)**

**Response:**
```json
{
  "id": "req-xxx",
  "status": "pending",
  "createdAt": "2026-03-18T..."
}
```

### `GET /api/activation-requests?all=1`
رؤية جميع طلبات التفعيل (Admin فقط)

### `PATCH /api/activation-requests`
الموافقة/الرفض على طلب (Admin فقط)

**Body:**
```json
{
  "id": "req-xxx",
  "status": "approved"
}
```

---

## 👨‍💼 لوحة تحكم Admin

### الوصول:
زر Admin في لوحة المقابض → Activation Requests

### الخيارات:
- ✅ **Approve** - الموافقة على الطلب
- ❌ **Reject** - رفض الطلب
- 📄 **View Details** - رؤية تفاصيل كاملة

---

## 🔔 الإخطارات

### عند رفع طلب:
```
✓ تم إرسال طلب التفعيل
جاري فحص الموافقة...
عدد الفحوصات: 1, 2, 3...
```

### عند الموافقة:
```
✅ تم تفعيل حسابك!
يمكنك الآن اللعب بدون قيود.
```

---

## ⚠️ ملاحظات أمان

1. **لا يمكن تخطيه:** النظام إجباري لجميع المستخدمين
2. **لا توجد backdoors:** حتى الـ URL مباشرة محمية
3. **التفعيل المزدوج:** لا يمكن رفع طلبات متعددة
4. **Audit Trail:** جميع الطلبات مسجلة

---

## 🐛 استكشاف الأخطاء

### المشكلة: لا أرى modal التفعيل
**الحل:**
- تحقق من أنك ليس Admin
- أعد تحميل الصفحة (F5)
- امسح cache المتصفح

### المشكلة: الفحص لا يتحدث تلقائياً
**الحل:**
- انقر زر "تحديث يدوي"
- امسح console errors
- تحقق من اتصالك بالإنترنت

### المشكلة: طلب الموافقة لا يظهر للـ Admin
**الحل:**
- تأكد من أنك Admin (ID صحيح)
- تحقق من `/api/activation-requests?all=1`
- افحص `data/activation-requests.json`

---

## 📈 الإحصائيات

```
📊 الطلبات المرسلة: X
✅ الطلبات المعتمدة: Y
⏳ الطلبات المعلقة: Z
❌ الطلبات المرفوضة: W
⏱️ متوسط وقت الموافقة: ~ دقائق
```

---

## 🎯 الخطوات التالية

- [ ] إضافة إشعارات Discord عند الطلب الجديد
- [ ] تذكيرات دورية للPlayers بدون طلب
- [ ] إحصائيات مرئية في Dashboard
- [ ] رسائل تخصيص بناءً على سبب الرفض

---

**آخر تحديث:** 18 مارس 2026
**النسخة:** 1.0
**الحالة:** ✅ جاهز للإنتاج
